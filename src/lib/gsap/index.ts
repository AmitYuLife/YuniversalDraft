/**
 * GSAP utilities and exports
 *
 * Re-export GSAP and plugins for consistent usage across the app.
 * Import from here instead of directly from 'gsap'.
 */

export { gsap } from "gsap";
export { ScrollTrigger } from "gsap/ScrollTrigger";
export { ScrollToPlugin } from "gsap/ScrollToPlugin";
